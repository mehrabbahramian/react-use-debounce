# react-use-debounce

A lightweight and reusable React hook for debouncing rapidly changing values.

Useful for search inputs, API requests, form validation, filtering, and other situations where you want to wait until a value stops changing before reacting to it.

